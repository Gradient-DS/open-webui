---
date: 2026-05-26T10:50:00+02:00
researcher: Lex Lubbers
git_commit: 15f5f3776b6b223df13e290839f14ab2df63095a
branch: feat/upstream-v0.9.5-merge
repository: open-webui
topic: 'v0.9.5 merge — fork-additions audit: did anything survive incompletely?'
tags: [v0.9.5-merge, audit, drift-detection, fork-additions, carveout]
status: complete
last_updated: 2026-05-26
last_updated_by: Lex Lubbers
type: audit
---

## TL;DR

**No fork features were dropped by the v0.9.5 merge.** Across 7 audit areas covering all 45 fork PRs between the prior upstream merge (`289e02c2a`, v0.8.12) and our Phase 0 commit (`9ca36d265`), every PR's signature files, env vars, routes, models, frontend components, migrations, helm templates, and CI workflows are present in HEAD `15f5f3776` with the documented Gradient-DS code paths intact.

Eight low-priority follow-ups surfaced (none blocking the v0.9.5 merge):

| # | Area | Priority | Item |
|---|---|---|---|
| 1 | Carve-out | medium | Port M4 (`MessageInput.svelte:356` focus-guard with `!$showCallOverlay`) — upstream bug-fix |
| 2 | Carve-out | medium (security-adjacent) | Port M9b (`MessageInput.svelte:2342` terminal-server permission gate) |
| 3 | Cloud sync | low | Verify `routers/integrations.py:640-647` `Knowledges.update_knowledge_by_id` no longer passes `access_control` (PR #119 intent drift?) |
| 4 | Cloud sync | low | Verify `routers/integrations.py:834` `file_id` prefix vs `services/sync/provider.py:43-47` `file_id_prefix_for(provider)` registry |
| 5 | Infrastructure | low | Document `OWUI_BE_PORT` / `WEAVIATE_HTTP_PORT` / `WEAVIATE_GRPC_PORT` / `POSTGRES_PORT` in `.env.example` (or confirm by-design env-builder-managed) |
| 6 | Infrastructure | low | Confirm `backend/start.sh` has no soev hooks by-design (audit prompt was speculative) |
| 7 | Branding | low | Verify `tailwind.config.js` minimalism is by-design consolidation into `src/tailwind.css` (PR #62 diff cross-check) |
| 8 | GDPR | low | Confirm PR #92 `proprietary-warnings` → `DataWarnings` is a rename, not a feature reduction (diff `97759ff32^..97759ff32`) |

Items 1+2 are concrete code follow-ups (single-file svelte edits, small surface). Items 3–8 are diff-confirmations that can be done in 5 minutes apiece.

# v0.9.5 Merge — Fork-Additions Audit

> **Purpose:** systematic check that every Gradient-DS fork customization landed between the prior upstream merge (v0.8.12, `289e02c2a`) and Phase 0 (`9ca36d265`) was preserved through the 3-way merge with upstream v0.9.5. Complement to the runtime smoke checklist — catches things smoke tests can't ("the button just isn't there").

## Scope

| Boundary | Commit | Date | What it represents |
|---|---|---|---|
| Prior upstream merge | `289e02c2a` | 2026-03-29 | "merge: merged open webui upstream from 260329" (PR #58). Last point where fork was synced with upstream. |
| Fork-side merge parent | `9ca36d265` | 2026-05-24 | "phase 0: upstream v0.9.5 merge prep". Our `dev` HEAD when the v0.9.5 merge happened. |
| Upstream-side merge parent | `3660bc00f` | 2026-05-?? | Upstream v0.9.5 release commit (the second parent of `03e143821`). |
| v0.9.5 merge commit | `03e143821` | 2026-05-24 | "Merge upstream open-webui v0.9.5 into dev" (the merge being audited). |
| Current HEAD | `15f5f3776` | 2026-05-25 | "agent: await get_event_emitter in streaming response" (12 latent-merge-bug fixups applied since the merge). |

**Audit range (fork side):** `289e02c2a..9ca36d265` = 201 non-merge commits, 45 feature-bearing PR merges.
**Audit range (upstream side):** `a2d40cdf9..3660bc00f` (upstream v0.8.12 state → upstream v0.9.5).
**High-risk overlap surface:** 234 code files where both fork and upstream made changes (i18n locales / docs / .claude meta excluded). 391 if locales+docs counted.

## Method

1. Enumerated 45 feature-bearing PR merges between the two upstream merges; categorized into 6 functional areas.
2. Computed file-touch overlap between the fork-side and upstream-side deltas to identify the 234-file high-risk drop zone.
3. Dispatched 7 parallel research subagents (6 area-drift detectors + 1 carve-out per-hunk auditor); each produced a verified/suspect/missing punch list.
4. Synthesized findings below. Specific file:line references where available.

## Fork PR inventory (45 PRs, by area)

### Agents (7 PRs)

| PR | Branch | Merge commit | Date |
|---|---|---|---|
| #107 | feat/generic-agents | `0f97ea841` | 2026-04-21 |
| #109 | feat/agent-selector | `9114c7097` | 2026-04-21 |
| #110 | feat/agent-api-key-secret | `29bc15279` | 2026-04-21 |
| #112 | hotfix/agent-api-agents-keyerror | `80d380344` | 2026-04-21 |
| #114 | feat/agent-picker | `bda4f5668` | 2026-04-28 |
| #131 | feat/auto-agent | `b8733fdc0` | 2026-05-17 |
| #135 | feat/simple-assistant-builder | `3479a7e3e` | 2026-05-24 |

### Cloud sync + integrations (4 PRs)

| PR | Branch | Merge commit | Date |
|---|---|---|---|
| #115 | feat/confluence-integration | `d674ae956` | 2026-04-28 |
| #116 | feat/sync-redis | `aadda776e` | 2026-04-30 |
| #119 | fix/external-int-acl | `219857f7f` | 2026-05-07 |
| #134 | feat/confluence-admin | `f8b9bdf49` | 2026-05-22 |

### Generative UI + streaming + RAG filter (5 PRs)

| PR | Branch | Merge commit | Date |
|---|---|---|---|
| #120 | feat/neo-filter | `a9e60ebf1` | 2026-05-11 |
| #124 | feat/streaming-logic | `1762906ab` | 2026-05-13 |
| #127 | feat/generative-ui | `3bbd9ce41` | 2026-05-16 |
| #128 | feat/mkbot-present-ui | `f9d9d7cd5` | 2026-05-16 |
| #130 | feat/filter-ui-update | `52876bb23` | 2026-05-17 |

### GDPR / data control / 2FA / acceptance (4 PRs)

| PR | Branch | Merge commit | Date |
|---|---|---|---|
| #61 | feat/email-2fa | `988dcd57e` | 2026-?? |
| #66 | feat/dpia | `b08fb6d4e` | 2026-?? |
| #92 | feat/proprietary-warnings | `4b12eeb40` | 2026-04-10 |
| #118 | feat/welcome-message | `e0f82c24e` | 2026-05-05 |

### Branding + tenant + docs (12 PRs)

| PR | Branch | Merge commit | Date |
|---|---|---|---|
| #59 | feat/logos | `0bd7a53d1` | 2026-?? |
| #62 | feat/aesthetic | `d228b1cdd` | 2026-?? |
| #71–87 | feat/vink ×9 | (9 merges) | 2026-?? |
| #93 | feat/word-pdf-copy | `97759ff32` | 2026-04-11 |
| #96 | feat/doc-writer | `91adf03ef` | 2026-04-14 |
| #121 | docs/contributing | `a60509a43` | 2026-05-12 |
| #125 | feat/configurable-docx | `c826b2014` | 2026-05-13 |

### Infrastructure / dev tooling / security (7 PRs + 3 tail commits)

| PR | Branch | Merge commit | Date |
|---|---|---|---|
| #106 | fix/ha-error-local-k8s | `69ae334b4` | 2026-04-20 |
| #111 | fix/session | `929a603f5` | 2026-04-21 |
| #117 | feat/staging-improvements | `a9718178e` | 2026-05-03 |
| #126 | fix/trivy-scan | `152a78ded` | 2026-05-13 |
| #129 | fix/bugs-staging | `84925e786` | 2026-05-17 |
| #136 | feat/multi-stack-dev-environment-v1 | `04e46b4bf` | 2026-05-24 |
| #137 | fix/security-scans | `eef4a4f14` | 2026-05-24 |
| — | env-driven vite proxy / WEBUI_HOSTNAME / location.host (3 commits) | `13e318979`, `3cf5ab935`, `cc09cc931` | 2026-05-24 |

## Findings

### 1. Agent proxy + agent picker

**Status:** CLEAN — 0 missing, 0 suspects across 7 PRs.

**Verified present:**
- **PR #107 generic-agents** — `backend/open_webui/utils/external_agents.py:46-381` (`get_repo_install_path`, `install_external_package`, `create_wrapper_content`, `extract_agent_metadata`, `register_external_agent_direct`, `load_external_agents_at_startup`). Startup hook at `main.py:903-905`. Admin UI at `src/lib/components/admin/Settings/ExternalAgents.svelte` wired via `Settings.svelte:34,764`.
- **PR #109 agent-selector** — `env.py:946` (`AGENT_API_AGENTS`), `config.py:3745-3761` (`AGENT_API_SELECTED_AGENT`, `AGENT_API_PICKER_DEFAULT_SLUG`), selector endpoints at `routers/configs.py:1121-1166`.
- **PR #110 agent-api-key-secret** — `env.py:940` (`AGENT_API_KEY`); used in `utils/agent.py:178-180` (`_agent_api_headers`), `routers/agent_proxy.py:172-174` (`_auth_headers`), `utils/service_auth.py:104-163` (`AgentPrincipal`).
- **PR #112 hotfix/agent-api-agents-keyerror** — defensive parse at `env.py:946`.
- **PR #114 agent-picker** — `routers/agent_configs.py:45-177` (full CRUD + reorder), `models/agent_configs.py:28-44` (`is_active`/`is_beta`/`position`/`meta` + access_grants), mounted at `main.py:2103`. Frontend `src/lib/apis/agent-configs/index.ts:65-119`, admin panel `Settings/Agents.svelte`, placeholder cards `chat/Placeholder/AgentCards.svelte` via `Placeholder.svelte:34,291`. `FEATURE_AGENT_PICKER` at `env.py:980` exposed via `main.py:3284`.
- **PR #131 auto-agent** — `AgentBadge.svelte` (Navbar at `Navbar.svelte:26,134`), `chat.meta.agent_id` write at `Chat.svelte:53,3150-3154,3436-3437`, backend resolution + access check at `main.py:2690-2754`, `override_agent` honored in `utils/agent.py:288-336`.
- **PR #135 simple-assistant-builder** — `FEATURE_SIMPLE_ASSISTANT_BUILDER` at `config.py:2043`, exposed via `main.py:3240`, frontend at `AssistantWizard.svelte` referenced from `workspace/models/create/+page.svelte:21,26` and edit page. Tests at `src/lib/utils/features.test.ts:62-73`.
- **Agent proxy itself** — `routers/agent_proxy.py:241-360` exposes `/models`, `/chat/completions`, `/gradient_agent_meta`, `/openapi.json`; collection-ref resolver `:83-139`; `ENABLE_AGENT_PROXY` gate `:142-162`; mounted at `main.py:2102`.

### 2. Cloud sync + integrations

**Status:** CLEAN — 0 missing, 2 low-priority suspects across 4 PRs.

**Verified present:**
- **All 4 sync service subtrees intact:** `services/sync/` (10 files inc. `base_worker.py`, `constants.py`, `events.py`, `pending_flows.py`, `pipeline_client.py`, `provider.py`, `router.py`, `scheduler.py`, `token_refresh.py`), `services/confluence/` (10 files inc. `confluence_client.py`, `basic_auth.py`, `auth.py`, `sync_worker.py`, `html_renderer.py`, `provider.py`, `scheduler.py`), `services/onedrive/`, `services/google_drive/` (parallel sets).
- **Phase 1.5 async conversions confirmed:** `services/sync/events.py:9,29,49` (`emit_file_processing`, `emit_file_added`, `emit_sync_progress` all `async def`); `services/sync/router.py:63,73,145,174,196,253,267,296,325,394` (10 async handler helpers); `services/sync/scheduler.py:64` (`_run` async loop, `start(app)` at `:41`); `services/confluence/sync_worker.py:112,212,225,252,267` (`ConfluenceSyncWorker(BaseSyncWorker)` with full async surface).
- **Routers wired in `main.py`:** L91, L117-119 (imports `integrations`, `onedrive_sync`, `google_drive_sync`, `confluence_sync`); L2096, L2130-2137 (mounted); L968-985 + L1086-1103 (3 schedulers lifespan-hooked); L3252-3271 (`enable_*_sync` flags in app config).
- **PR #116 sync-redis** — `services/sync/pending_flows.py:1-50` Redis-backed cross-replica OAuth pending-flow store with in-process fallback; key prefix from `REDIS_KEY_PREFIX:18`; `_redis(request)` reads `request.app.state.redis` (`:30`).
- **PR #119 external-int-acl** — `routers/integrations.py:77-109` (`get_integration_provider` enforces user.info.integration_provider binding + INTEGRATION_PROVIDERS registration); endpoints depend on `get_integration_principal`/`get_verified_user` (`:570,780,823`); LoaderPrincipal vs push-provider branching (`:586-599`); cross-provider deletion guard at `:797,831`. `utils/service_auth.py:36-50` `LoaderPrincipal` dataclass + `:59-100` `get_integration_principal` bearer/header machine-auth path. `config.py:3685` `INTEGRATION_PROVIDERS = PersistentConfig(...)`.
- **PR #134 confluence-admin** — `routers/confluence_sync.py:17-32` imports all 34 `CONFLUENCE_*` config entries; `services/confluence/basic_auth.py` + `auth.py` present.
- **Frontend:** `src/lib/components/admin/Settings/Integrations.svelte:35,483` (imports/renders `IntegrationProviders.svelte`), `IntegrationProviders.svelte` present, `src/lib/apis/sync/index.ts:7-46` types intact, `IntegrationsMenu.svelte` present.

**Suspects (low priority):**
- `routers/integrations.py:640-647`: `Knowledges.update_knowledge_by_id` call builds `KnowledgeForm(name=..., description=..., type=...)` but comment at `:639` says "Update access_control on existing KB" — the form does NOT pass `access_control`. Verify against PR #119 original intent (likely intentional, but worth a one-line diff check).
- `routers/integrations.py:834`: `file_id = f'{provider}-{document_source_id}'` uses slug-as-prefix directly instead of `file_id_prefix_for(provider)` (the latter maps `google_drive` → `googledrive-`). May diverge from `services/sync/provider.py:43-47` prefix registry.
- `main.py:3152` imports `_find_shared_kb` from `routers/confluence_sync` (private import across module boundary) — verify still exists.

**Deferred (per prior handoff, not in this audit's scope):** per-file Weaviate collections regression — will rebase onto dev's `e64b083b9` post-merge.

### 3. Generative UI + streaming + RAG filter

**Status:** CLEAN — 0 missing across 5 PRs. 2 minor notes (architectural-by-design).

**Verified present:**
- **PR #124 streaming-logic** — All 5 custom event branches intact in `_build_streaming_response.body_generator` (`backend/open_webui/utils/agent.py`):
  - `status` — L434-447
  - `source` — L449-462
  - `present_ui` — L464-479
  - `context_usage` — L481-497
  - `panel_filter` — L499-517
  - Recent fix `15f5f3776` is applied: `event_emitter = await get_event_emitter(metadata)` at L428.
  - `done` sentinel at L265-267, L431-432.
- **PR #128 present-ui dispatcher** — `src/lib/components/chat/Messages/Markdown/PresentUIDispatcher.svelte:1-122` wired into `ResponseMessage.svelte:64,105,1069-1072`. Backing stores `submitPromptSignal` at `stores/index.ts:122`, `choiceBlockRegistry` at `stores/index.ts:124-131`. `ChoiceBlock.svelte` co-located.
- **Frontend chatEventHandler in `Chat.svelte`** dispatches all 5 event types:
  - `present_ui` → uiBlocks build at L622-640
  - `context_usage` → L641-655
  - `panel_filter` → L656-666
  - `source`/`citation` → L667+
  - Subscribe `$socket?.on('events', chatEventHandler)` at L847; teardown at L979.
- **PR #120 / #130 RAG filter UI** — `RagFilter.svelte`, `RagFilterPanel.svelte` present; `src/lib/stores/rag-filter.ts:1-53` (`ragFilterState`, `showRagFilter`, `updateRagFilter`, `resetRagFilter`); imported by `Chat.svelte:120-121`, `MessageInput.svelte:113,1854`, `RagFilterPanel.svelte:23`, `utils/rag-filter.ts:9,29`. Feature flag `enable_rag_filter_ui` plumbed: `config.py:3505-3508` → `main.py:3191` → frontend gates at `Chat.svelte:3391`, `MessageInput.svelte:1849`. Outbound payload `rag_filter: getRagFilterForRequest()` at `Chat.svelte:2788`; backend strips at `main.py:2462`; forwarded by `utils/agent.py:370`.
- **panel_filter downstream rendering** — `Document.svelte:29,306` and `ResponseMessage.svelte:1083` accept `panelFilter`; `Citations.svelte:23-33` documents suppression; `Chat.svelte:1300-1301` attaches per-message filter.
- **IFRAME_CSP** — `config.py:1439`, exposed at `main.py:3362` (`iframe_csp`), consumed at `Artifacts.svelte:249`, `FullHeightIframe.svelte:197`, `FilePreview.svelte:415`.
- **StatusHistory** — `src/lib/components/chat/Messages/ResponseMessage/StatusHistory.svelte:1-40+`; merged with reasoning via `ResponseMessage.svelte:71,187,255,275`.

**Minor architectural notes (by-design, not regressions):**
- `socket/main.py:937-945` `get_event_emitter` emits to `room=f'user:{user_id}'`, not a dedicated `'events'` room. The `'events'` string at L938 is the Socket.IO **event name**, not a room. The single-per-user room receives these events. Matches upstream behavior — if PR #124 ever intended a separate events room, that was never added (works regardless).
- `rag-filter` store is imported directly via `$lib/stores/rag-filter`, not re-exported from `stores/index.ts` barrel — module-direct pattern, consistent across all consumers.

### 4. GDPR / data control / 2FA / acceptance

**Status:** CLEAN — 0 missing across 4 PRs + Phase 1.5 cascade.

**Verified present:**
- **PR #61 email-2FA / TOTP** — `models/auths.py:28` `totp_secret` column + `:231-238` `update_totp_secret_by_id`; `routers/auths.py:82` imports `compute_twofa_grace`, `:170-191` 2FA grace+enrollment guard, `:726-735` recovery-code branch; `routers/totp.py`, `utils/totp.py` present; `src/lib/components/chat/Settings/Account/TwoFactorSetup.svelte` present.
- **Email helpers** — `services/email/auth.py`, `graph_mail_client.py` (Microsoft Graph client_credentials flow) present.
- **PR #66 DPIA / retention / archival / deletion** — `services/archival/{__init__,service}.py` present; `services/deletion/{service,cleanup_worker}.py` present (cleanup task wired at `cleanup_worker.py:22 start_cleanup_worker` / `:30 stop_cleanup_worker` / `:39 _run_cleanup_loop`); `services/retention/{service,config}.py` present (TTL resolution, `is_retention_enabled`); `models/user_archives.py`; `routers/archives.py`.
- **Migrations** — `f8e1a9c2d3b4_add_user_archive_table.py`, `d4e5f6a7b8c9_add_soft_delete_columns.py`, `f1e2d3c4b5a6_add_access_grant_table.py`, `eaa33ce2752e_create_invite_table.py` all present.
- **Phase 1.5 async cascade delete-by-user-id (14+ methods)** — `models/chats.py:1571 delete_chats_by_user_id`, `:1643 delete_shared_chats_by_user_id`, `:1797 soft_delete_by_user_id`; `models/knowledge.py:861 soft_delete_by_user_id`; plus tags, tools, memories, prompts, functions, channels, notes, folders, groups, oauth_sessions, models, messages, feedbacks (all `async def ..._by_user_id` confirmed). Wired in `services/deletion/service.py:370-535`.
- **Invites (async)** — `routers/invites.py:77 async def create_invite`, `:230 async def accept_invite`, `:347 async def list_invites`; frontend `src/routes/auth/invite/[token]/+page.svelte`, `src/lib/components/admin/Users/UserList/InviteUserForm.svelte`.
- **PR #92 data warnings** — `src/lib/components/workspace/Models/DataWarnings.svelte` (file_upload/web_search/knowledge_local labels, config-guarded by `enable_web_search`/`enable_image_generation`/`enable_code_interpreter`); consumed by `Chat.svelte`, `ModelEditor.svelte`, `ModelSettingsModal.svelte`.
- **PR #118 welcome + acceptance** — `src/lib/components/chat/WelcomeMessage.svelte`, `chat/Placeholder.svelte`; `layout/Overlay/AcceptanceModal.svelte`; `admin/Settings/Acceptance.svelte`; mounted in `src/routes/(app)/+layout.svelte`. `chat/Settings/DataControls.svelte` present.

**Suspects (low priority):**
- PR #92 in-code naming is `DataWarnings` / `warningConfigGuards` — no literal "proprietary" string remains in `src/`. Likely a rename during the PR's lifecycle, not a drop; one-line diff confirmation: `git log --diff-filter=A --follow src/lib/components/workspace/Models/DataWarnings.svelte`.
- `models/knowledge.py` only has `soft_delete_by_user_id` (line 861), no hard `delete_knowledges_by_user_id`. `models/files.py` only has `get_files_by_user_id` (line 243), no `delete_files_by_user_id`. Deletion service uses `soft_delete_by_user_id` for KBs/Chats and per-id calls for Files — verify against handoff's "5 GDPR delete methods" list (the count may include soft-delete + cascade + per-resource-type methods rather than a single deleter per model).

### 5. Branding + tenant + docs

**Status:** CLEAN — 0 missing across 12 PRs (including 9 Vink merges).

**Verified present:**
- **PR #59 logos** — `static/static/gradient-logo.png` (the green/teal Gradient mesh-surface logo) + `static/static/{logo.png, splash.png, splash-dark.png, favicon.{ico,svg,png}, favicon-96x96.png, favicon-dark.png, apple-touch-icon.png, web-app-manifest-{192,512}*.png, site.webmanifest}` all present. "Powered by soev.ai" + `<img src="/static/gradient-logo.png">` references intact at `src/routes/auth/+page.svelte:657-662`, `admin/Settings/General.svelte:312`, `chat/Settings/About.svelte:185`. Dutch translation at `i18n/locales/nl-NL/translation.json:1869`.
- **PR #62 aesthetic** — `src/app.css:3-31` custom `@font-face` declarations (Inter, Archivo, Mona Sans, InstrumentSerif, Vazirmatn loaded from `/assets/fonts/...`); `src/app.css:33-60` `--app-text-scale` UI-scale CSS variable + sidebar item sizing; `src/tailwind.css:6-17` custom oklch-based `--color-gray-*` palette (50–950, plus 850).
- **PR #93 word-pdf-copy** — `ResponseMessage.svelte:1315,1323-1335` `downloadMessageAsPdf()` + `downloadMessageAsDocx()` (gated on `enable_docx_export`).
- **PR #96 doc-writer** — Backend flag `config.py:2668-2682` (`ENABLE_DOCUMENT_WRITER`, `DOCUMENT_WRITER_PROMPT_TEMPLATE` PersistentConfig); plumbing at `main.py:184,1720,3194`, `utils/tools.py:531`; frontend `chat/Document.svelte`, `Messages/Markdown/DocumentCard.svelte`; capability mapping `utils/assistantCapabilities.ts:14,25,38,54`, `utils/capabilities.ts:19-20`; helm at `values.yaml:322-323`, `configmap.yaml:173`.
- **PR #125 configurable-docx** — Backend flag `config.py:2055-2058` (`ENABLE_DOCX_EXPORT`, default True); 403 enforcement at `routers/utils.py:152` (`/chat/docx`) + `:201` (`/document/docx`); exposed to frontend at `main.py:598,3248` (`features.enable_docx_export`); 5 frontend gates: `ChatMenu.svelte:333`, `Navbar/Menu.svelte:451`, `Document.svelte:264`, `ResponseMessage.svelte:1323`, `DocumentCard.svelte:165`; helm at `values.yaml:328`.
- **PR #121 docs/contributing** — `docs/agent-api-deployment.md` + `docs/deployment-demo.md` present.
- **PR #71-#87 feat/vink (9 merges)** — Vink-era cloud-sync code under generic names in `services/sync/`, `services/google_drive/`, `services/onedrive/`, `services/confluence_*` (covered in §2). Helm flags at `values.yaml:417-464`. Soev chat agent variants seeded by migration `d6e7f8a9b0c1_add_soev_chat_variants.py`. Literal "Vink" string appears only in `thoughts/` research notes — intentional: Vink-tenant-specific overrides live in the separate `soev-gitops` repo, not in `open-webui` source.

**Suspect (low priority):**
- `tailwind.config.js` (lines 1-30) contains only upstream `extend.typography`, `padding.safe-bottom`, `transitionProperty.width`. No fork-specific theme tokens here — colours/fonts live in `src/tailwind.css` + `src/app.css`. If PR #62 added entries to `tailwind.config.js` historically, they were consolidated into CSS during the v0.9.5 merge (or earlier). Likely by-design but worth diff-confirming against the PR #62 merge (`d228b1cdd^..d228b1cdd -- tailwind.config.js`).

### 6. Infrastructure / dev tooling / security

**Status:** CLEAN — 0 missing across 7 PRs + 3 tail commits.

**Verified present:**
- **PR #136 multi-stack dev (+ tail commits)** — `vite.config.ts:23-39` env-driven proxy reading `OWUI_BE_PORT` (default `8080`) with `/api`, `/ollama`, `/openai`, `/oauth`, `/static` AND the critical `/ws` entry with `ws: true` at L36 (the `88c379d39` restore). `vite.config.ts:46-48` env-driven esbuild pure-console stripping. `src/lib/constants.ts:13-14` `WEBUI_HOSTNAME = browser ? (dev ? location.host : '') : ''` (commits `cc09cc931` + `3cf5ab935` intact). `docker-compose.soev-stack.yaml:1-40` per-stack compose with `${WEAVIATE_HTTP_PORT:?…}`, `${WEAVIATE_GRPC_PORT:?…}`, `${POSTGRES_PORT}` references; header comment references `scripts/stack/stack.py` + `.env.stack-N`.
- **PR #126 / #137 security scans** — `.trivyignore:1-20` 4 CVE allowlist entries with rationale (CVE-2026-23949, -24049, -44721, -45395). `.github/workflows/security-scanning.yaml:1-98` full pipeline: Bandit SAST (L12), pip-audit + 4 ignore-vuln entries (L28-53), npm audit (L55-68), Trivy with `severity: CRITICAL,HIGH`, `exit-code: '1'`, `ignore-unfixed: true` (L70-98). `Dockerfile:127-141` security-fix `--only-upgrade` block for libcap2/libsystemd0/libudev1/libgnutls30. `utils/security_headers.py:7-33` pure ASGI-3 `SecurityHeadersMiddleware`; imported + registered in `main.py:732,1912`.
- **PR #111 session fixes** — `utils/auth.py:222-275` two-mechanism token-revocation (per-`jti` + per-user `revoked_at` for OIDC back-channel logout); `:312-413` cookie-based `token` extraction + deletion of `token`, `oauth_id_token`, `oauth_session_id` on signout. `routers/auths.py:65,101-127` `invalidate_token` imported + `create_session_response` honors `expires_at_cap` (2FA cap). `.env.example:108-111` `ENABLE_OAUTH_BACKCHANNEL_LOGOUT=True` documented as Gradient default.
- **PR #106 HA k8s** — `socket/main.py:65-115` module-import-time `AsyncRedisManager` removal preserved; `init_websocket_redis_manager()` lifespan hook intact with research-note reference. `:124-168` lazy redis client + sync `RedisDict`/`RedisLock` split.
- **PR #117 / #129 staging improvements + bug fixes** — `Dockerfile:7,53,204-213` `USE_PERMISSION_HARDENING` build arg + OpenShift arbitrary-UID hardening block; `Dockerfile:152-156` `USE_SLIM=true` branch (`requirements-slim.txt`, no torch). `.github/workflows/pr-checks.yaml:1-124` frontend build/test, ruff format check, backend pytest, helm-lint + helm-template, slim docker build no-push. `.github/workflows/branch-guard.yaml:1-31` dev→test→main promotion enforcement.
- **Helm tenant chart (Gradient-authored)** — `helm/open-webui-tenant/Chart.yaml:1-23` (version 1.0.6, Gradient DS maintainer). All 27 templates present: open-webui (deployment/service/configmap/pvc/pdb/migration-job), postgres, redis, weaviate, loader-worker (6 files inc. networkpolicy + bootstrap-job), cilium-networkpolicy, external-secrets, cluster-secret-store, ingress, serviceaccount, secrets. `values.yaml:1-145` tenant identification, shared-services wiring, `Recreate` default strategy + rationale, migrationJob + PDB sections intact. `.github/workflows/docker-build-soev.yaml:177-232` chart publish job to `oci://ghcr.io/gradient-ds/charts` with `chart-v*` tag handling.
- **Other** — `.pre-commit-config.yaml:1-7` ruff-format on `backend/`.

**Suspects (low priority):**
- `.env.example` does NOT document `OWUI_BE_PORT`, `WEAVIATE_HTTP_PORT`, `WEAVIATE_GRPC_PORT`, or `POSTGRES_PORT`. Runtime reads them (`vite.config.ts:29`, `docker-compose.soev-stack.yaml:36-37`) and stack-manager writes them into `.env.stack-N` — likely intentional (env-builder-managed) but no human-discoverable hint in `.env.example`. Decision: leave as-is, or add `# Multi-stack dev — auto-set by stack manager in .env.stack-N` block.
- `backend/start.sh` has no soev-specific startup hooks; the audit prompt was speculative ("Gradient may have added startup hooks here") — file looks fully upstream. Likely never had hooks; no action needed unless someone wanted to add e.g. permission-fixup or env-preflight steps.
- `Dockerfile:43` uses `npm ci --force` (matches `pr-checks.yaml:24`) — soev convention being preserved.

### 7. Carve-out per-hunk decisions

**Files audited:**
- `src/lib/components/chat/MessageInput.svelte` (currently at `--ours`)
- `src/lib/components/chat/MessageInput/InputMenu.svelte` (currently at `--ours`)

**Saved upstream-side diffs:** `thoughts/shared/research/v0.9.5-carveout-diffs/`

#### MessageInput.svelte — per-hunk decision table

| # | Diff lines | Upstream change | Fork location | Decision | Rationale |
|---|---|---|---|---|---|
| M1 | 9 | Import `TaskList` from `Messages/ResponseMessage/TaskList.svelte` | imports near L99–111 | NEUTRAL | Pure addition; needed only if M7 ported. Component exists. |
| M2 | 17–20 | Reactive `isActive = taskIds?.length>0 \|\| !done \|\| generating` | new var after L138 | NEUTRAL | Refactor extracting condition used at L2298. No behavior change unless M8 also adopted. |
| M3 | 29 | New `export let chatTasks = []` prop | after L158 | SKIP-UX | Only consumed by M7. |
| **M4** | **38–41** | **Guard `chatInputElement?.focus()` with `!$showCallOverlay` in `setText`** | **L355–356** | **PORT** | **Bug fix: prevents focus-stealing from voice/call overlay when programmatic text is set. Non-UX, store already imported (L28). Trivial 3-line change at L356.** |
| M5 | 49–50 | Add `':'` to `showCommands` trigger list (emoji shortcode) | L424–429 | SKIP-UX | Fork rewrote this block with `isFeatureEnabled` gates; would conflict. |
| M6a | 58–61 | New `terminalCapableModels` reactive | L516–522 area | NEUTRAL | Capability-gating helper; only needed by M9. |
| M6b | 71–73 | Clear `$selectedTerminalId` when no model supports terminal | new `$:` after L564 | NEUTRAL | Cleanup analogous to existing L562–564 (`codeInterpreterEnabled` reset). Low-risk QoL bug-prevention. |
| M7 | 108–113 | Render `<TaskList tasks={chatTasks}>` above queued messages | around L1256 in upstream / fork L~2100 area | SKIP-UX | Tied to upstream task system; not wired in fork. |
| M8 | 122–135 | Filter-pill X-click opens ValvesModal if `has_user_valves` | L2142–2146 | NEUTRAL | Behavior change: clicking pinned filter pill with user-valves opens valves editor instead of removing the filter. Functional improvement, not pure UX. Worth a follow-up. |
| M8b | 143–155 | Inner X-mark `<div>` gets `on:click` removing filter (with `stopPropagation`) | around L2168–2170 | NEUTRAL | Co-dependent with M8. |
| M9a | 163 | Stop button condition `isActive && prompt==='' && files.length===0` | L2298 | SKIP-UX | Hides Stop while user is typing during generation. Fork keeps Stop always-visible — explicit design call. |
| **M9b** | **172–176** | **Terminal menu gated by `terminalCapableModels.length > 0` AND direct-tool-server permission split for `t.id` vs admin/permission** | **L2342** | **PORT (security-adjacent)** | **Permission gate: non-admins without `features.direct_tool_servers` permission can no longer see ad-hoc terminal servers (those without `id`) or settings-defined `terminalServers`. Server-registered tool servers (`t.id`) remain visible to all. Worth porting for permission consistency.** |

#### InputMenu.svelte — per-hunk decision table

| # | Diff lines | Upstream change | Fork state | Decision |
|---|---|---|---|---|
| I1 | 9 | Import `Files.svelte` | not imported, file exists | SKIP-UX (unless I4 ported) |
| I2 | 17 | `show = false` after `uploadFilesHandler()` | already present (L321) | NO-OP |
| I3 | 25 | `show = false` after capture handler | already present (L365) | NO-OP |
| I4 | 33 | `show = false` after `showAttachWebpageModal = true` | already present (L414) | NO-OP |
| I5 | 41–71 | New "Attach Files" submenu entry (sets `tab = 'files'`) | not present | SKIP-UX (Files-tab attach feature) |
| I6 | 80 | `show = false` after Google Drive handler | already present (L493) | NO-OP |
| I7 | 88–105 | `{:else if tab === 'files'}` panel rendering `<Files {onSelect}/>` | not present | SKIP-UX |
| I8 | 113 | `show = false` after OneDrive personal | already present (L549) | NO-OP |
| I9 | 121 | `show = false` after OneDrive organizations | already present (L581) | NO-OP |

#### Carve-out summary

- **MessageInput.svelte:** 1 clear PORT (M4 focus guard), 1 PORT-recommended (M9b permission gate, security-adjacent), 4 NEUTRAL, 4 SKIP-UX. **Overall:** keep fork `--ours`; cherry-port M4 + M9b as small follow-up commits.
- **InputMenu.svelte:** 0 PORT, 0 NEUTRAL, 2 SKIP-UX (Files-tab feature), 6 already-present (all `show = false` close-on-click hunks fork added independently). **Overall: keep fork `--ours` as-is — no action.**
- **Security-relevant findings:** M4 = focus-stealing bug fix during voice-call overlay (not a vuln); M9b = permission-enforcement fix (consistent with how other direct-tool-server gates work elsewhere). No XSS, sanitization, or MIME-validation hunks in either diff — the prior handoff's "no security hunks" note holds for those categories.

## Cross-cutting follow-ups

The audit found **no merge-induced feature regressions**. Eight low-priority items emerged; none block Phase 5 (push / PR / tag).

### Recommended ports (small commits on this branch)

1. **`MessageInput.svelte:356`** — port M4 focus-guard:
   ```diff
   - chatInputElement?.focus();
   + if (!$showCallOverlay) chatInputElement?.focus();
   ```
   ~3 lines. Single-file change inside `setText`. Bug fix for focus-stealing during voice-call overlay.

2. **`MessageInput.svelte:2342`** — port M9b terminal-server permission gate. Restrict ad-hoc terminal entries (without `id`) and settings-defined `terminalServers` to admins / users with `features.direct_tool_servers` permission. Server-registered (`t.id`) entries remain visible to all. Single-file change in the terminal menu render block.

Both can land as one commit (`[carveout-port] MessageInput.svelte: focus guard + terminal permission gate`) or two — handoff guidance suggests one `[carveout-port]` commit per file documenting all per-hunk decisions.

### Diff-confirmations (5 min each, deferrable)

3. **`routers/integrations.py:640-647`** — `Knowledges.update_knowledge_by_id` doesn't pass `access_control` despite the comment saying so. Run `git log -p 219857f7f^..219857f7f -- backend/open_webui/routers/integrations.py` to confirm PR #119 intent.
4. **`routers/integrations.py:834`** — verify `file_id = f'{provider}-{document_source_id}'` matches the slug-registry at `services/sync/provider.py:43-47` (`file_id_prefix_for(provider)`).
5. **`.env.example`** — add a multi-stack-dev hint block, or document that `OWUI_BE_PORT` / `WEAVIATE_*_PORT` / `POSTGRES_PORT` are env-builder-managed (decide which).
6. **`tailwind.config.js`** — confirm PR #62 consolidated theme tokens into `src/tailwind.css` rather than `tailwind.config.js` (`git log -p d228b1cdd^..d228b1cdd -- tailwind.config.js`).
7. **PR #92 DataWarnings** — confirm rename from `proprietary-warnings` is the intended shape (`git log --diff-filter=A --follow src/lib/components/workspace/Models/DataWarnings.svelte`).
8. **`backend/start.sh`** — confirm no soev-specific startup hooks were ever expected. If yes, file a separate ticket.

### Open issues carried forward from prior handoffs (NOT new audit findings)

These are tracked in `v0.9.5-phase-4c-smoke-handoff-2.md` and `v0.9.5-phase-4c-smoke-handoff.md` — not re-discovered by this audit:

- Per-file Weaviate collections regression (deferred to rebase on dev's `e64b083b9`).
- `STATIC_DIR` clearing at import (`config.py:927-936`) — long-term guard candidate.
- Empty agent response on one Nebul turn — Nebul / model misbehavior, not OWUI.
- genai-utils `env_builder.py` follow-ups (`CORS_ALLOW_ORIGIN`, `PROVIDER_OPENWEBUI_DIRECT_URL`, `OPENAI_API_KEY`) — separate genai-utils ticket.

## What this audit does NOT cover

- **Runtime behavior** — that's the Phase 4c smoke checklist (see `v0.9.5-phase-4c-onwards-handoff.md` and `-handoff-2.md`).
- **Async-conversion correctness** — Phase 1.5 audit already covered the model methods; this audit looks at *whether the features themselves are still wired up*, not whether every async coroutine is awaited.
- **Tests beyond static existence** — pytest runs are part of Phase 4b validation, not this audit.
- **Vink tenant-specific helm values in soev-gitops** — that's a different repo.
- **In-flight branches** (`feat/vink`, `feat/bim-agent`, `feat/email-2fa`, etc.) — out of scope per the prior handoff.

## Re-entry / next steps after this audit

Once findings are filled in:
1. Triage each **Missing / dropped** item — decide port now (commit on this branch) vs defer to follow-up PR.
2. Triage each **Suspect** item — quick spot check; either promote to confirmed-present or to missing.
3. For carve-outs: the per-hunk decisions become the body of two `[carveout-port]` commits per the Phase 5 guidance.
4. Update the v0.9.5 PR description with audit summary (X items verified, Y items required follow-up, Z carve-out decisions documented).
