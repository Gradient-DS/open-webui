### Current State

<!-- Mutable — update frequently, remove items when resolved. See methodology.md Section 6. -->

#### Current Work

- **KB frontend upstream convergence (P2-8, Option A)** — branch `feat/kb-upstream-convergence` (worktrees in open-webui + genai-utils). Plan: `docs/superpowers/plans/2026-07-23-kb-frontend-upstream-convergence.md` (in the OWUI worktree). **Phase 1 (backend: directory materialization + rollups + delete-cascade parity) committed, awaiting staging manual verification. Phase 2 (frontend re-base of KnowledgeBase/Files/AddContentMenu onto upstream v0.10.2 directory UI, local KBs only, kill-switch `kbUpstreamUi`) implemented 28-07-2026, uncommitted; first manual round done, follow-ups fixed same day (multi-select drag-move; pre-existing session-expiry SPA navigation loop in auth/+page.svelte `$user !== undefined` gate; daemon-era `/sync/items` now rolls back 'syncing' + 502 on SyncDaemonError; OneDrive picker /api/config caching).** Phase 3 (cloud KB chrome on new scaffold: upstreamUiActive all types, source-root rows via sources[].root_directory_id + utils/sourceMap.ts, rollup badges, fileBadge status icons, 2s-coalesced level refresh + 4s sync poll, KB-wide quota total, cloud drop gating) implemented 28-07-2026, uncommitted, awaiting manual verification. Also fixed: daemon pre-run failures (e.g. token needs_reauth 404) now post a terminal failed summary to OWUI (genai-utils run.py execute_kb_sync) — no more 30-min 'syncing' wedge; OneDrive e2e VERIFIED 28-07-2026 17:26 (bg-sync OAuth done → daemon full sync: 16 files, 2 source-root dirs, correct placement; delta re-run green). Daemon-path root_directory_id stamping gap fixed in routers/sync_daemon.py (terminal summaries stamp folder sources by root-dir name, self-healing; 25 router + 161 daemon tests green). Dev-stack daemon now runs as a DETACHED nohup process (wrapper: scratchpad run-sync-daemon.sh, auto-restart) because parallel stackctl-dev live checks (stack-2, branches chore/stackctl-editable-install-live-check + feat/sync-daemon-test-stackctl-tui) kept killing the stack-1 sync-daemon tmux window. Phase 4 (legacy tree + /tree,/search endpoints + kill-switches DELETED) done 28-07-2026. SHIPPED: open-webui PR #242 (Phases 2-4 + auth-loop fix + sync robustness) + genai-utils PR #355 (daemon pre-run terminal summaries). v0.11.0 merge dry-run: KnowledgeBase.svelte conflicts 409→133 lines (-67%). Remaining: staging soak + PR review/merge, then cleanup_feature. Dev-stack gotchas: sync-daemon tmux window can be missing — recreate with `./.venv/bin/python -m services.sync_daemon.app.main` in the genai-utils worktree (source .env, apps/soev/.env, .env.stack-1); OneDrive sync on this branch REQUIRES the daemon on :18110 (loader-worker orchestration is gone from /sync/items). Do-not-restore ledger (no file rename/content editor/pending-poll/unlink-delete) applied; latent `onSync`/`onReset` AddContentMenu defects fixed by construction.
- **Upstream v0.10.2 merge** — merge of upstream/main (v0.9.5 → v0.10.2, 639 commits) initiated on `feat/upstream-v0.10.2-merge` (worktree `.worktrees/feat/upstream-v0.10.2-merge`, merge LEFT IN PROGRESS, 148 conflicts). Full resolution plan: `thoughts/shared/plans/2026-07-02-upstream-v0.10.2-merge.md` (10 analysis agents, per-file recipes, locked decisions D1–D12). Two dominant work items: (1) upstream DELETED PersistentConfig/AppConfig/app.state.config → async per-key `Config` model; ~200+ fork read sites (~111 in non-conflicted fork-only files) must migrate + all custom keys re-registered in DEFAULT_CONFIG (dict-valued `integrations.providers` needs a migration keep-intact guard or it's shredded); (2) upstream `knowledge_directory` table collides with our path-based KB subfolders — keep ours as UX, adopt their schema additively. Carve-outs again: `routers/retrieval.py` (+2 utils shims), `KnowledgeBase.svelte`, `socket/main.py`, `utils/redis.py`. NOTE: the v0.9.5 merge is DONE — landed in dev 2026-05-26 via PR #140 (branch `fix/merge-0.9.5`); the prior state entry claiming phases 2–5 were pending was stale. **Dev env ready (02-07-2026): stack-1** (`tmux attach -t stack-1`; owui-fe 18173 / owui-be 18180 / agent 18181 / weaviate 18182 / postgres 18132) with idle `claude` window (Fable, nothing queued) in the OWUI merge worktree; execution = per-phase kickoff, subagent-driven dev (Fable orchestrator, Opus subagents). Bootstrap workarounds documented in the plan's Execution model (ours-side dep files in working tree, `.pth`+stub instead of editable install).
- **Sync-daemon rework (design stage)** — replace OWUI-internal cloud sync (~17k LOC: services/sync/**, 4 connector services, sync routers, schedulers, loader-worker orchestration) with a stateless per-tenant sync-worker daemon speaking upstream's sync protocol (`/sync/diff` + `/sync/cleanup` + upload path), vanilla-oikb compatible. **Sequencing decided: v0.10.2 merge lands FIRST** (it ships upstream's protocol endpoints natively), daemon work after on the merged base. Decisions (02-07-2026): (1) FULLY adopt upstream's `knowledge_directory` folder model asap post-merge (backfill from `relative_path`, converge UI; `source_item_id` stays for provider mapping); (2) **warren is the ONLY acceptable parse route** — no doc-processor fallback in upload path; required format list = the 27 tenant `allowed_extensions` (pdf/doc/docx/dotx/xls/xlsx/xltx/ppt/pptx/potx/csv/tsv/txt/text/md/markdown/rtf/odt/ods/odp/html/htm/xml/json/epub/msg/rst) — warren must reach parity on these, custom formats (ifc) out of scope; (3) daemon = new service in genai-utils, retire loader-worker — **inspection DONE (02-07-2026)**: loader-worker is single-purpose (only OWUI cloud-sync jobs; safe to retire), warren already fetches presigned S3 GETs itself via `resolve_http` (no new resolver needed; verify dp-worker image ships `warren[http]==0.2.2`, presign TTL > queue backlog), 3 loader-worker behaviors must re-home (token-refresh retries, Workspace MIME export, `SourceAccessRevoked`→KB delete), format parity 14/27 (+`htm` allowlist one-liner; 12 formats need warren parsers — see factoids).
- **Confluence Cloud Sync admin panel** — implementing `thoughts/shared/plans/2026-05-21-confluence-cloud-sync-admin.md` (3 phases). **Phase 1 done + extended, awaiting manual verification**: new Cloud Sync admin tab with Confluence + Google Drive + OneDrive sections (all OAuth config admin-editable, no Helm/restart needed); `/api/v1/configs/{confluence,google_drive,onedrive}` GET/POST endpoints; all three sync routers mounted unconditionally + scheduler runtime-toggleable; OneDrive/GDrive enable toggles removed from Documents tab (Integration section deleted); env-only creds promoted to PersistentConfig. Scope was expanded mid-Phase-1 at user request to migrate OneDrive/GDrive too (plan amended). Phases 2 (Confluence username+API-token Basic auth) + 3 (shared read-only full-content KB) pending. Driving use case: unblock Intermax (Confluence Cloud, one shared KB for all users). **Note:** v0.9.5 merge work is on a worktree, so `dev` stays clean for verifying Confluence Phase 1.
- **Weaviate native multi-tenancy — Phase 1 done (open-webui), flag OFF** — `feat/weaviate-tenancy` branch (open-webui primary checkout). New `weaviate_multitenancy.py` MT connector + `_weaviate_mt_mapping.py` + factory flag-branch + dual-read shim; `ENABLE_WEAVIATE_MULTITENANCY_MODE` (default off) / `WEAVIATE_MT_LEGACY_FALLBACK` (default true). 4 commits, 59 targeted tests green, behavior-neutral with flag off. Phase 2 (genai-utils adapter) also done (see shared-knowledge). **Pending:** local manual verification (flag OFF baseline → flag ON new-KB tenant creation → dual-read over existing KBs; recipe in the 03-06-2026 note); Phases 3–6 (gitops: 1.37.7 upgrade, migration Job, cutover, rollout) NOT started — out of scope. Branches not yet pushed/PR'd. See note `[03-06-2026] Weaviate Native Multi-Tenancy — Phase 1`.
- **Vink deployment** — large-scale client deployment (~1300 docs), active on `feat/vink` branch. Core sync worker issues resolved, plans created for next steps (single-collection architecture, batch-wait removal)
- External agents package — active development and integration
- New cloud integrations (Google Drive recently completed, exploring more)

#### Pending Fixes

- **Cloud KB permission leak — code fix done, cleanup pending**: `_sync_permissions()` rewritten (owner-only check + suspension lifecycle). Still need to: (1) deploy code fix to gradient.soev.ai, (2) run grant cleanup SQL AFTER deploy.

#### Completed Recently

- **Sync worker performance overhaul** — concurrent document processing in `base_worker.py`, unified frontend upload concurrency (01-04-2026)
- **Vink large-scale fixes** — asyncio.gather chunking, `DOCUMENT_PROCESSING_TIMEOUT`, parsing upgrades, sync logging (03–06-04-2026)
- **DPIA compliance** — merged via PR #66. Data export, data retention, file upload improvements, zip export security fix, cloud sync hardening.
- **TOTP 2FA Phase 1** — merged via PR #61. Phase 2 (email OTP) deferred.
- **Security hardening** — Trivy CVE fixes, Docker slimming, TOTP replay token fix
- **Aesthetic polish** — merged via PR #62. KB logos, tab split, Dutch translations

#### Open Questions

#### Active Resources

- Sync abstraction cookbook: `collab/docs/external-integration-cookbook.md`
