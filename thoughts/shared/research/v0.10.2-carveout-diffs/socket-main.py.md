# `backend/open_webui/socket/main.py` — Upstream v0.10.2 Carve-out Port Decisions

**Summary**: 17 upstream logical hunks reviewed — 12 PORTED, 5 DELIBERATELY-SKIPPED (all part of the D5 HA loop-safety carve-out), 0 N/A. No `PersistentConfig` / `app.state.config` dead references found (config-migration clean). No crash risk found; `get_event_emitter` / `get_event_call` are intact and functionally verified — generative-UI event dispatch is safe.

Refs: merge-base `3660bc00f`, upstream `19ce61c7b^2`, fork tip `19ce61c7b^1`, merged `19ce61c7b` (== working tree). Cross-checked against `git diff 19ce61c7b^1 19ce61c7b -- backend/open_webui/socket/main.py`, which confirms the merge only ever touched the security/behavior hunks below — the HA architecture hunks show zero diff between fork-tip and merged (i.e. truly carved out at "ours", untouched by the merge).

## Per-hunk decision table

| # | Area / function | Upstream change | Decision | Evidence (merged file:line or "absent") | Rationale |
|---|---|---|---|---|---|
| 1 | Module imports (`@@ -1,55 +1,50 @@`) | Full import reorg: `from __future__ import annotations`, alphabetized import blocks, drops unused `Set`, moves `redis.asyncio as aioredis` import, imports `build_sentinel_url`/`get_sentinels_from_env` (new names) from `utils.redis`, adds `is_valid_token` to the `utils.auth` import | **DELIBERATELY-SKIPPED** (style) / **PORTED** (substance) | absent: no `from __future__ import annotations` (L1); imports still in fork's original unsorted layout with a duplicated second `from open_webui.env import (GLOBAL_LOG_LEVEL,)` block (L16-19, L25-41, L51-53). **But** `is_valid_token` import IS present: `from open_webui.utils.auth import decode_token, is_valid_token` (L42) | D5: fork keeps its own import layout/style rather than reformatting wholesale. The functionally-relevant piece of this hunk — importing `is_valid_token` — was cherry-picked in support of hunks 6-9 below. Cosmetic reordering has zero behavior impact and was left alone to minimize merge-diff noise. |
| 2 | `AsyncRedisManager` construction (`@@ -63,20 +58,20 @@`) | Renames local var `mgr`→`redis_manager`; refactors the sentinel/non-sentinel ternary to use new `build_sentinel_url()`; still constructs `socketio.AsyncRedisManager(...)` and passes it as `client_manager=` **at module-import time** inside the `if WEBSOCKET_MANAGER == 'redis':` block that also builds `sio` | **DELIBERATELY-SKIPPED** (D5: HA loop-safety) | absent: merged file builds `sio = socketio.AsyncServer(...)` with **no** `client_manager=` kwarg at all (L77-87). Redis-backed manager is instead attached later by `init_websocket_redis_manager()` (L90-115), called from the FastAPI lifespan (`backend/open_webui/main.py:550-552`) | This is exactly the fork's core HA fix: constructing `AsyncRedisManager` at import time binds its internal asyncio primitives to whatever loop is current at import (not uvicorn's serving loop), causing "got Future attached to a different loop" errors under HA (replicaCount > 1). See `thoughts/shared/research/2026-04-20-redis-ha-loop-bug-and-kind-repro.md`. Upstream's refactor of this block was necessarily skipped wholesale since the fork's architecture removed the code it modifies. |
| 3 | `ws_sentinels` dedup (`@@ -104,32 +99,31 @@`) | Computes `ws_sentinels = get_sentinels_from_env(...)` once and reuses it for `REDIS`, `MODELS`, `SESSION_POOL`, `USAGE_POOL` (previously recomputed via a second `redis_sentinels = get_sentinels_from_env(...)` line) | **N/A** (superseded by D5 lazy-REDIS refactor) | absent: merged file computes sentinels **twice** — once inside the `lazy(lambda: get_redis_connection(..., redis_sentinels=get_sentinels_from_env(...)))` for `REDIS` (L130-137), and again as `redis_sentinels = get_sentinels_from_env(...)` (L139) reused by the four Redis-backed pools/locks below | Fork wraps `REDIS` in `lazy()` so it resolves on uvicorn's running loop at first access rather than at import (same root HA fix as hunk 2). This restructuring makes upstream's single-dedup harder to apply verbatim; net behavior is identical (same sentinel list computed either once or twice, no functional difference), so the minor duplicate-call inefficiency was left as-is rather than risk touching the lazy-REDIS wiring. |
| 4 | `redis_sentinels`→`ws_sentinels` rename, `clean_up_lock` (`@@ -137,7 +131,7 @@`) | Pure variable rename for the dedup above | **N/A** (cosmetic) | absent: merged file still uses `redis_sentinels` (L166) | Purely a variable-name consequence of hunk 3's dedup, which was not ported. Zero behavior difference. |
| 5 | `redis_sentinels`→`ws_sentinels` rename, `session_cleanup_lock` (`@@ -148,7 +142,7 @@`) | Same rename, second call site | **N/A** (cosmetic) | absent: merged file still uses `redis_sentinels` (L177) | Same as hunk 4. |
| 6 | `connect()` token validity check (`@@ -348,9 +342,12 @@`) | Extracts the per-request `redis` client via `environ['asgi.scope']['app'].state.redis` (falling back to module `REDIS`), and adds `await is_valid_token(data, redis)` to the auth condition | **PORTED** | `backend/open_webui/socket/main.py:378-384` — `scope = (environ or {}).get('asgi.scope') or {}`, `redis = getattr(getattr(fastapi_app, 'state', None), 'redis', None) or REDIS`, `if data is not None and 'id' in data and await is_valid_token(data, redis):` | Genuine security hardening (session/token revocation check), no conflict with HA architecture — merits-based port. `is_valid_token` confirmed present in `backend/open_webui/utils/auth.py:240`. |
| 7 | `user_join()` token validity check (`@@ -371,15 +368,19 @@`) | Same `is_valid_token` addition; also simplifies `auth = data['auth'] if 'auth' in data else None` → `auth = data.get('auth')` | **PORTED** | `backend/open_webui/socket/main.py:404,408-414` — `auth = data.get('auth')` (L404) and the same `is_valid_token(token_data, redis)` guard (L413) | Same security fix as hunk 6, applied to a second handler; the `.get()` simplification was carried along since it's part of the same hunk. |
| 8 | `join_channel()` token validity check (`@@ -422,8 +423,12 @@`) | Same `is_valid_token` addition | **PORTED** (substantively) | `backend/open_webui/socket/main.py:459-465` — redis/is_valid_token block added; **but** note L455 still reads `auth = data['auth'] if 'auth' in data else None` (old style, NOT simplified to `.get()` the way hunk 7 was) | The security-critical `is_valid_token` check was ported; the cosmetic `.get()` simplification from the same upstream hunk was not carried over here, leaving a minor stylistic inconsistency between handlers. No functional impact — both forms behave identically. |
| 9 | `join_note()` token validity check (`@@ -444,8 +449,12 @@`) | Same `is_valid_token` addition | **PORTED** | `backend/open_webui/socket/main.py:487-491` | Same security fix, third handler. |
| 10 | `yjs_document_leave` — authenticated identity (`@@ -763,11 +772,13 @@`) | Requires `user = SESSION_POOL.get(sid)` (rejects unauthenticated sessions); replaces client-supplied `data.get('user_id', sid)` with the authenticated `user['id']` for logging | **PORTED** | `backend/open_webui/socket/main.py:806-814` — `user = SESSION_POOL.get(sid); if not user: return`, `log.info(f'User {user["id"]} leaving document {document_id}')` | Closes an auth-bypass / spoofing hole (client could claim to be any `user_id` on leave). Pure security hardening, no HA conflict. |
| 11 | `yjs_document_leave` — broadcast payload (`@@ -775,10 +786,10 @@`) | Continuation of hunk 10: the `ydoc:user:left` broadcast now emits the authenticated `user['id']` instead of the client-supplied value | **PORTED** | `backend/open_webui/socket/main.py:822-827` — comment "user_id is the authenticated identity, not client-supplied" + `{'document_id': document_id, 'user_id': user['id']}` | Same fix as hunk 10, second half of the same function. |
| 12 | `yjs_awareness_update` — auth + room-membership check (`@@ -793,16 +804,21 @@`) | Requires authenticated `SESSION_POOL` entry; normalizes `document_id`; requires the emitting session to have actually joined `doc_{document_id}` (`room not in sio.rooms(sid)`) before broadcasting; uses authenticated `user['id']` instead of client-supplied | **PORTED** | `backend/open_webui/socket/main.py:838-856` | Same class of auth-bypass fix as hunks 10-11, extended with a room-membership check (prevents broadcasting awareness updates into a document you never joined). No HA conflict. |
| 13 | `disconnect()` signature (`@@ -811,7 +827,7 @@`) | `async def disconnect(sid):` → `async def disconnect(sid, reason=None):` | **PORTED** | `backend/open_webui/socket/main.py:863` — `async def disconnect(sid, reason=None):` | Required by the newer `python-socketio` server API, which now passes a `reason` to disconnect handlers. Non-optional — omitting this would raise a `TypeError` on every disconnect once the `python-socketio` dependency is bumped. Confirmed present. |
| 14 | `_make_channel_emitter` — channel_id validation (`@@ -845,13 +861,16 @@`) | Fetches `msg = await Messages.get_message_by_id(message_id)` once up front and validates `msg.channel_id == channel_id` before proceeding (previously fetched only inside the `done` branch, with no channel ownership check) | **PORTED** | `backend/open_webui/socket/main.py:897-906` — `msg = await Messages.get_message_by_id(message_id); if not msg or msg.channel_id != channel_id: return` then reuses `msg.meta` in the `done` branch | Data-integrity/security fix (prevents cross-channel message updates) plus a minor efficiency win (single fetch reused for the `done` meta-merge). No HA conflict. |
| 15 | `get_event_emitter` — `chat_id` null-safety, channel branch (`@@ -899,7 +918,7 @@`) | `request_info.get('chat_id', '').startswith('channel:')` → `(request_info.get('chat_id') or '').startswith('channel:')` | **PORTED** | `backend/open_webui/socket/main.py:954` | Defensive fix for the case where `chat_id` key is present but explicitly `None` (the old `.get(key, default)` form only substitutes the default when the key is *absent*, not when its value is `None`) — would otherwise raise `AttributeError: 'NoneType' object has no attribute 'startswith'`. |
| 16 | `get_event_emitter` — `chat_id` null-safety, `local:` branch (`@@ -917,7 +936,7 @@`) | Same null-safety pattern applied to the `local:` prefix check | **PORTED** | `backend/open_webui/socket/main.py:972` | Same defensive fix, second call site. |
| 17 | `get_event_call` — session ownership check (`@@ -1021,9 +1040,10 @@`) | Replaces the fast-fail `if session_id not in SESSION_POOL:` with an ownership check: `session = SESSION_POOL.get(session_id); if session is None or session.get('id') != request_info.get('user_id'): ...` | **PORTED** | `backend/open_webui/socket/main.py:1076-1080` | **Security-critical**: `session_id` is client-supplied (via `request_info`), and without this check a malicious/compromised caller could target *any* live session (not just their own) for an `event_call` round-trip (used for tool-confirmation / interactive prompts in generative-UI streaming). Confirmed present and matches upstream exactly. |

## Event system (`get_event_emitter` / `get_event_call`) verdict

**Working — no break, no upstream architectural rework detected in this file.**

- `get_event_emitter(request_info, update_db=True)` still emits event name **`'events'`** to room **`f'user:{user_id}'`** (`backend/open_webui/socket/main.py:962-970`) — exactly what the fork's generative-UI streaming client subscribes to. Channel-mode routing (`_make_channel_emitter`) and chat-persistence branches (`status`/`message`/`replace`/`embeds`/`files`/`source`/`citation`) are all intact and unchanged in shape.
- `get_event_call(request_info)` still calls back via `sio.call('events', ..., to=session_id, timeout=WEBSOCKET_EVENT_CALLER_TIMEOUT)` (`backend/open_webui/socket/main.py:1083-1092`), with `get_event_caller = get_event_call` alias preserved (`:1103`).
- The only changes upstream made to these two functions in v0.10.2 were the two `chat_id` null-safety guards (hunks 15-16, defensive, non-breaking) and the session-ownership hardening in `get_event_call` (hunk 17, security-only, does not change the calling contract — legitimate callers targeting their own session are unaffected). **No rework of the event-emission mechanism itself** (event name, room naming scheme, or call signature) was found in the upstream diff for this file, so there is nothing bigger to have "skipped" here.
- Verified the corresponding fork-only lifespan hooks (`init_websocket_redis_manager()`, `clear_connection_cache()`) are wired into `backend/open_webui/main.py` startup (lines 550-552 and 475 respectively), so the HA carve-out is not just present in code but actually invoked.

## Crash risk findings

- **None.** `grep -n "app.state.config\|PersistentConfig"` against this file returns zero matches — no dead references to the removed `PersistentConfig`/`app.state.config` system.
- `is_valid_token` (imported L42) is confirmed defined in `backend/open_webui/utils/auth.py:240`.
- `get_sentinel_url_from_env` (used by `init_websocket_redis_manager`, L106) is confirmed still exported from `open_webui.utils.redis` as a back-compat wrapper (see companion `redis.py.md`), so the HA lifespan hook does not import a name that no longer exists.
- Two harmless dead imports remain from the pre-merge fork state and were **not** touched by this merge: `from typing import Dict, Set` (`Set` is unused) and `from redis import asyncio as aioredis` (unused — the async redis import used at runtime goes through `open_webui.utils.redis.get_redis_connection`, not this direct import). Pre-existing lint noise, not a merge regression, not a crash risk.

## Risks / follow-ups

1. **Stylistic inconsistency in hunk 8**: `join_channel()` still uses the old `data['auth'] if 'auth' in data else None` idiom while `user_join()` (hunk 7) was updated to `.get('auth')`. Both are functionally equivalent (data is already known to be a dict from earlier validation) — cosmetic only, no action required unless a future lint/consistency pass touches this file.
2. **Dead imports** (`Set`, `aioredis`) could be cleaned up opportunistically but are pre-existing and out of scope for this carve-out audit (per project memory: pre-existing lint noise is not to be chased).
3. **`ws_sentinels` dedup (hunks 3-5) not ported**: purely a micro-optimization (one extra `get_sentinels_from_env()` call at module import). Zero runtime cost of consequence (runs once at process start). Safe to leave as-is; could be folded into a future pass if the HA lazy-REDIS block is ever revisited.
4. No action items block this merge from the socket/main.py side — all upstream security/bugfix hunks were successfully ported, and the fork's HA-loop-safety architecture (module-import-time `AsyncRedisManager` removal, lazy `REDIS`, `init_websocket_redis_manager()` lifespan hook, sync `RedisDict`/`RedisLock` split) remains fully intact and correctly wired.
