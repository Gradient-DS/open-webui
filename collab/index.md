<!-- Append-only — one row per note. See methodology.md Section 3 for amendment protocol. -->

| Date | Who | Title | Summary | Keywords |
|------|-----|-------|---------|----------|
| 20-03-2026 | @lexlubbers | Gradient-DS Custom Features Overview | Documented all 9 custom features built on top of Open WebUI: Typed KBs, OneDrive, Email Invites, GDPR Archival, Acceptance Modal, Feature Flags, Feedback Config, External Pipeline/Integration Providers, and Agent API. All survived the v0.6.43 → v0.8.9 upstream merge. | custom-features, upstream-merge, onedrive, feature-flags, agent-api, knowledge-types, gdpr, invites |
| 24-03-2026 | @lexlubbers | External Integration Cookbook | Step-by-step recipe (12 steps) for adding new cloud sync providers to the sync abstraction layer. Covers backend (config, API client, auth, token refresh, sync worker, provider, factory, router, main.py) and frontend (API client, picker, KnowledgeBase.svelte). Includes architecture diagram, abstract method reference, provider comparison table. | sync-abstraction, integration-cookbook, BaseSyncWorker, SyncProvider, cloud-sync, google-drive, onedrive |
| 26-03-2026 | @lexlubbers | Google Drive Integration — Implementation & Sync Abstraction Refactor | Full Google Drive integration: OAuth+PKCE auth with callback multiplexing, Drive API v3 client, sync worker with Changes API for incremental sync, Workspace file export. Refactored OneDrive code into shared sync abstraction layer (BaseSyncWorker with 11 abstract methods). | google-drive, sync-abstraction, oauth, changes-api, workspace-export, google-picker, BaseSyncWorker, token-refresh |
| 26-03-2026 | @lexlubbers | Agent Proxy — External API for soev.ai Agents | Reverse proxy at `/api/v1/agent/` exposing agent service to external callers with sk- key auth. Separate from `AGENT_API_ENABLED` (internal chat routing). Reuses `AGENT_API_BASE_URL`, follows Ollama/OpenAI proxy pattern (aiohttp + stream_wrapper). Admin toggle, inline docs with OpenAPI download, Helm chart support. Also fixed Vite dev proxy for all API routes. | agent-proxy, ENABLE_AGENT_PROXY, openai-compatible, streaming, helm, vite-proxy |
| 26-03-2026 | @lexlubbers | Feature Flags for Webpage URL and Reference Chats | Added `FEATURE_WEBPAGE_URL` and `FEATURE_REFERENCE_CHATS` env vars + Helm config to allow disabling these two InputMenu items that previously had no feature flag guard. Full pipeline: Helm → configmap → config.py → main.py → features.ts → InputMenu.svelte. | feature-flags, InputMenu, FEATURE_WEBPAGE_URL, FEATURE_REFERENCE_CHATS, helm |
| 30-03-2026 | @lexlubbers | Cloud KB Permission Leak — Sync Workers Mirror Cloud Sharing | `_sync_permissions()` in OneDrive/Google Drive sync workers maps cloud folder sharing to Open WebUI access grants on every sync cycle, leaking private KBs to all team members as read-only. Root cause: sync workers call model layer directly, bypassing router defense-in-depth. Fix: rewrite to only verify owner access, not mirror sharing. Migration NULL→private fix is separate valid hardening. | _sync_permissions, access_grants, permission-leak, sync-worker, onedrive, google-drive, base_worker, defense-in-depth, knowledge-visibility |
